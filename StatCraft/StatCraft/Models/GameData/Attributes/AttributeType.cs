namespace StatCraft.Models.GameData.Attributes
{
    // Shared by build attributes and map attributes — the value shapes an attribute can take.
    public enum AttributeType { Numeric, Bool, Percent, Values }
}
